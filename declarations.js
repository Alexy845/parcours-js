const escapeStr = "`" + "\\" + '/' + '"' + "'"
const arr = new Array[4,'2']

const obj = {
    str: 'string',
    num: 5,
    bool: true,
    undef: undefined,
}

const nested = {
    arr: [4,undefined,'2'],
    obj: {
        str: 'string',
        num: 5,
        bool: true,
    }
}
